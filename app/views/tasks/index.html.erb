<h1>My Tasks Home-page</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task)%> </li>
  <% end %>
</ul>
<p><%= link_to "Add a new Task", tasks_new_path(@tasks)%></p>
